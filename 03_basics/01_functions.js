// Function Use For Particular Task To Repetative at any Location in code if it is in There Scope.

// It We Use Parameter in Function But Not It Get added in The Calling Time Then It Gets Undefined vaule. 
// function loginUser(user)
// {
//     return `${user} just logged in`
// }

// It Value Not Assign.
// function loginUser(user)
// {
//     if(user === undefined)  // if(!user) Same as Above.
//     {
//         console.log("Please Enter a User Name...")
//         return 
//     }
//     return `${user} just logged in`
// }
//console.log(loginUser())

